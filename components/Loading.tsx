import React from "react";
import { Text } from "react-native";
import { textStyles } from "../styles/textStyles";

export default function Loading() {
  return <Text style={textStyles.regular}>Loading...</Text>;
}
