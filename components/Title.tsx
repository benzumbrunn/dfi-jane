import React from "react";
import { Text } from "react-native";
import { textStyles } from "../styles/textStyles";

export default function Title() {
  return <Text style={textStyles.title}>DFI Jane</Text>;
}
